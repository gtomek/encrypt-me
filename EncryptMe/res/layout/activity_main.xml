<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <TextView
        android:id="@+id/encryption_type_title"
        android:text="@string/encryption_type_title" />

    <TextView
        android:id="@+id/encryption_type"
        android:layout_toRightOf="@id/encryption_type_title" />

    <TextView
        android:id="@+id/key_desc_title"
        android:layout_below="@id/encryption_type_title"
        android:text="@string/key_description_title" />

    <TextView
        android:id="@+id/key_value"
        android:layout_below="@id/encryption_type_title"
        android:layout_toRightOf="@id/key_desc_title" />

</RelativeLayout>